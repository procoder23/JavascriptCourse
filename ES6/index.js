//ES6 Module = An external file that contains reusable code that can be imported into other javascript file.
//Write reusable code for many different apps.

import {PI, getCircumference, getArea, getVolume} from './mathUtils.js';

console.log(PI);

const area = getArea(6);
console.log(area);